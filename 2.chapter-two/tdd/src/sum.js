/**
 * Método para realizar a soma de dois números
 * @param {*} a - Primeiro valor
 * @param {*} b - Segundo valor
 * @returns
 */
function sum(a, b) {
  return a + b;
}

module.exports = {
  sum,
};
