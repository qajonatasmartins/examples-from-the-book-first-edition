const { describe, it } = require("mocha");

describe("Produto XPTO", () => {
  it("[CT-0001] - Realizar login com credenciais válidas", () => {
    /* ============================================================= */
    /* Arrange (Organizar) */
    /* 1. Passo: Abrir o navegador */
    /* 2. Passo: Clicar no botão [Login] da página inicial */
    /* ============================================================= */
    /* Act (Agir) */
    /* 3. Passo: Informar o e-mail */
    /* 4. Passo: Informar a senha */
    /* 5. Passo: Clicar no botão [Entrar] */
    /* ============================================================= */
    /* Assert (Afirmar)*/
    /* 6. Passo: Checar se o nome do usuário logado é igual a "Usuário XPTO" */
    /* ============================================================= */
  });
});
