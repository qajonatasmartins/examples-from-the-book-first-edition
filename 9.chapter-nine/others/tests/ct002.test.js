const { describe, it } = require("mocha");

/**
 * Regra de valor mínimo para saque em um sistema bancário, onde o valor mínimo de saque é 3.000 reais por dia.
 * Usando o Test Design de Analise de valor limite para criar os casos de testes.
 */
describe("Produto XPTO", () => {
  it("[CT-002] - Validar um saque de 3.000,00 (exatamente no limite)", () => {
    /* Arrange (Organizar) */
    /* Act (Agir) */
    /* Assert (Afirmar)*/
  });
});
