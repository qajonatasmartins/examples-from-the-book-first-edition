const { describe, it } = require("mocha");

/**
 * O describe representa o nível mais alto do seu teste,
 * aqui constumo utilizar o nome do SUT (Sistema sobre teste).
 */
describe("Produto XPTO", () => {
  /**
   * O arquivo it representa o seu teste.
   * Para uma boa rastreabilidade é importante vincular o código de seu
   * caso de teste criado na sua ferramenta de gerenciamento de teste.
   */
  it("[CT-0001] - Realizar o cadastro de pessoa física", () => {
    /* Arrange (Organizar) */
    /* Act (Agir) */
    /* Assert (Afirmar)*/
  });
});
